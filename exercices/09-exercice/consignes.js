/**
 *  Faire une fonction anonyme qui affiche la soustraction de 2 nombres
 */

let soustraction = function(nb, nb2) {
    let resultat = nb - nb2;
    console.log(`${nb} - ${nb2} = ${resultat}`);
}

soustraction(10, 4)