/**
 *  Faire une fonction fléchée qui affiche la soustraction de 2 nombres
 */
let soustraction = (nb, nb2) => {
    let resultat = nb - nb2;
    console.log(`${nb} - ${nb2} = ${resultat}`);
}

soustraction(44, 120)

